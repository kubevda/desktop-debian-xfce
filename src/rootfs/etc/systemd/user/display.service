[Unit]
Description=Xvnc Display

[Service]
Type=simple
Restart=always
EnvironmentFile=/etc/default/kubevda
ExecStart=/usr/bin/Xvnc ${DISPLAY} -rfbunixpath ${DISPLAY_SOCK_ADDR} -SecurityTypes None

[Install]
WantedBy=default.target
